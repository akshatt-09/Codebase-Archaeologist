# Development Guide

1. Ensure Python 3.10+ and Node.js 18+ are installed.
2. Start Flask with `python backend/app.py`.
3. In another terminal, run `npm run dev` in `frontend/`.
4. Run `pytest` before submitting changes.